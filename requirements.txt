# --- API ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# --- leitura dos documentos-fonte ---
pypdf>=5.1.0            # extração de texto do PDF de políticas
reportlab>=4.2.0        # geração do PDF a partir do markdown-fonte

# --- provedores de LLM ---
httpx>=0.27.0           # Groq e Gemini via REST

# O SDK da OCI fica em requirements-oci.txt. São mais de 100 MB de dependência
# para um provedor que só entra em ação com OCI_COMPARTMENT_ID preenchido, e o
# import dele é preguiçoso — sem o pacote, o provedor apenas se declara
# indisponível e o roteador segue para o próximo.
